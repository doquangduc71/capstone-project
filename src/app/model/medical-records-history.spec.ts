import { MedicalRecordsHistory } from './medical-records-history';

describe('MedicalRecordsHistory', () => {
  it('should create an instance', () => {
    expect(new MedicalRecordsHistory()).toBeTruthy();
  });
});
